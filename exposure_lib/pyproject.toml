[tool.poetry]
name = "exposure_lib"
version = "0.1.0"
description = "Exposure scoring pipelines"
authors = ["Alex"]

[tool.poetry.dependencies]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
